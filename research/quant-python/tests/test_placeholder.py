"""Placeholder test — verifies the test suite is wired up correctly."""


def test_placeholder() -> None:
    """This test always passes. Replace with real tests as the package grows."""
    assert True
